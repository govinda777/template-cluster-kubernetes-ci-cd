apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: aws-provider-config
spec:
  credentials:
    source: WebIdentity
    webIdentity:
      # Integrated with EKS Pod Identity agent / OIDC automatically
      roleARN: arn:aws:iam::123456789012:role/crossplane-aws-provider-role
